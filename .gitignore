*.env
*env
code/backend/.env

# code/app é o Sistema B (Rivexx): gerado autonomamente pelo Dev Agent e
# versionado no próprio repositório git dele (GitWorkspace), separado deste -
# os dois sistemas nunca compartilham banco, processo ou código (docs/arquitetura.md §1).
code/app/

__pycache__/
*.pyc

