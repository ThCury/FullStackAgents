# Opcional - defaults já funcionam para rodar tudo localmente.
# Lista separada por vírgula (o Next cai para 3001 se a 3000 estiver ocupada).
FRONTEND_ORIGIN=http://localhost:3000,http://localhost:3001
BACKEND_PORT=8000
