name: fullstack-agents

services:
  mongo:
    image: mongo:8
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: fullstack_agents
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
